__pycache__/
*.pyc
.DS_Store
.env
/weights/
# Model weights are shared out of band, never committed: this one is 243 MB, well over
# GitHub's 100 MB file limit, so a stray `git add .` would fail the push outright.
/mobileclip2_*.ts
server_log.txt

# Trained checkpoints and datasets: large binaries, rebuildable from the recordings
act_ckpt/
act_final/
openyam_dataset/
openyam_all.db
weights/hand_landmarker.task
pipeline_log.txt
