# Medication check (server/doses.py): a local audit log and a local kill switch, never committed.
doses.jsonl
.dose_check_off

# Medication schedule history (server/schedule.py): patient data, never committed.
medications.jsonl

# Turns off only the schedule-driven reminders (server/doses.py); a local switch, never committed.
.schedule_reminders_off

# Hides the streak from the patient (server/doses.py); a local switch, never committed.
.streak_off
